import { createSlice } from "@reduxjs/toolkit";

const NicknameChangeSlice = createSlice({
    name: "Nicknamechanger",
    initialState: {},
    reducers: {},
    extraReducers: (builder) => {
        builder.addCase()
        builder.addCase()
        builder.addCase()
    }
})

export default NicknameChangeSlice