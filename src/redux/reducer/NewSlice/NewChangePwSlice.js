import { createSlice } from "@reduxjs/toolkit";

const PwChangeSlice = createSlice({
    name: "pwchanger",
    initialState: {},
    reducers: {},
    extraReducers: (builder) => {
        builder.addCase()
        builder.addCase()
        builder.addCase()
    }
})

export default PwChangeSlice